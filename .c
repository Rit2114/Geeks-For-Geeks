class Solution {
  public:
    vector<int> asciirange(string& s) {
        // code here
        int n = s.size();
        vector<int>ans;
        vector<int>freq(26, -1);
        vector<int>prefixsum(26, 0);
        int sum = 0;
    }
};
