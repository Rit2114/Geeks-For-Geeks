class Solution {
  public:
    int maxSum(vector<int> &arr) {
        // code here
         int maxi = 0;
        int n = arr.size();
        for(int i=0;i<n-1;i++)
        maxi = max(maxi, (arr[i]+ arr[i+1]));
        return maxi;
    }
};
